#include <iostream>
#include <math.h>
using namespace std;

int main(){
    int A, B, C, D, DIF;
    cin >> A;
    cin >> B;
    cin >> C;
    cin >> D;

    DIF = (A*B)-(C*D);

    cout << "DIFERENCA = " << DIF << endl;
    return 0;
}
